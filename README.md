# phishing-detection
This project is a machine learning-based web application that detects whether a given URL is a phishing website or a safe website. It analyzes different features of the URL and uses a trained model to make predictions. The system is built using Python and Flask, and it provides real-time results through a simple and user-friendly interface.
